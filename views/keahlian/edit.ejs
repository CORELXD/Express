<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bootstrap Demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #f8f9fa; /* Light Grayish Blue Background */
        }

        .container {
            background-color: #ffffff; /* White Container Background */
            border-radius: 8px; /* Rounded Corners */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Box Shadow */
            margin-top: 20px;
            padding: 20px;
        }

        form {
            max-width: 400px; /* Set Max Width for the Form */
            margin: 0 auto; /* Center the Form */
        }

        /* Add additional styles as needed */
    </style>
</head>
<body>

<form action="/keahlian/update/<%= id %>" method="POST" class="container">
    <div class="mb-3">
        <label for="nama" class="form-label">Nama Keahlian</label>
        <input type="text" class="form-control" name="nama_keahlian" value="<%= nama_keahlian %>" required/>
    </div>
    <div class="mb-3">
        <label for="tingkatan" class="form-label">Tingkatan</label>
        <select class="form-control" name="tingkat_keahlian" required>
            <option value="">Pilih Tingkatan</option>
            <option value="1" <%= tingkat_keahlian === '1' ? 'selected' : '' %>>1</option>
            <option value="2" <%= tingkat_keahlian === '2' ? 'selected' : '' %>>2</option>
            <option value="3" <%= tingkat_keahlian === '3' ? 'selected' : '' %>>3</option>
            <option value="4" <%= tingkat_keahlian === '4' ? 'selected' : '' %>>4</option>
            <option value="5" <%= tingkat_keahlian === '5' ? 'selected' : '' %>>5</option>
        </select>
    </div>
    <div class="mb-3">
        <label for="mahasiswa" class="form-label">Mahasiswa</label>
        <select class="form-control" name="id_mahasiswa" required>
            <option value="">Pilih Mahasiswa</option>
            <% for (let i = 0; i < data.length; i++) { %>
            <option value="<%= data[i].id_mahasiswa %>" <%= data[i].id_mahasiswa === id_mahasiswa ? 'selected' : '' %>><%= data[i].nama_depan %></option>
            <% } %>
        </select>
    </div>
    <div class="mb-3">
        <button type="submit" class="btn btn-sm btn-success">Simpan</button>
    </div>
</form>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
